{
  "name": "fracsys-demo-site",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npm --prefix webapp/html run dev",
    "build": "npm --prefix webapp/html run build",
    "build:preview": "npm --prefix webapp/html run build:preview",
    "preview": "npm --prefix webapp/html run preview",
    "type-check": "npm --prefix webapp/html run type-check",
    "postinstall": "npm --prefix webapp/html install"
  }
}
