{
  "name": "fracsys-flowviewer",
  "version": "1.0.0",
  "private": true,
  "description": "",
  "type": "module",
  "scripts": {
    "dev": "npm --prefix .. run dev",
    "build": "npm --prefix .. run build",
    "preview": "npm --prefix .. run preview",
    "type-check": "npm --prefix .. run type-check:flow"
  },
  "dependencies": {
    "papaparse": "^5.5.3",
    "three": "^0.184.0"
  },
  "devDependencies": {
    "@types/papaparse": "^5.5.2",
    "@types/three": "^0.184.0"
  }
}
