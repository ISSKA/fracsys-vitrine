# Template for local flow-viewer configuration. Copy this file to .env.local
# in the same directory and adjust it for your environment.
# Only VITE_-prefixed variables are exposed to the browser bundle by Vite.

# Grid CSV loaded from vitrine-flowviewer/public/data/.
VITE_DEFAULT_GRID_FILENAME=flow_network_voxels.csv
