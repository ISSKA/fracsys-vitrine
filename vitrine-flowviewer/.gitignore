node_modules
dist
backup
.claude
# Local env files hold secrets / machine-specific values — never commit them.
# Copy .env.example to .env and edit it locally.
.env
.env.local
.env.*.local
!.env.example
# Ignore generated data files, but keep the folder itself
public/data/*
!public/data/.gitkeep
